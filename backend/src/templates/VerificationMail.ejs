<!DOCTYPE html>
<html lang="en">

<head>
  <title>Email Verification</title>
  <meta charset="utf-8">
</head>

<body>
  <% if (name) { %>
    <p>Hi <%= name %>,</p>
    <% } %>
    <% if (company && link) { %>
    <p>We just need to verify your email address before you can access <%= company %>.
      <a href="<%= link %>">Verify your email address link</a>
      Thanks! – The <%= company %> team</p>
    <% } %> 
</body>

</html>